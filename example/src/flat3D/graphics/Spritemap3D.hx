package flat3D.graphics;

import com.haxepunk.Graphic.ImageType;
import com.haxepunk.graphics.Image;
import com.haxepunk.graphics.Spritemap;
import openfl.geom.Rectangle;
import openfl.geom.Vector3D;

/*
interface dreidable {
	public function Drei3 getdrei3()
}

new DreiD(NewClass Image()

class Spritemap3D extends Spritemap implements dreidable
{
	
	public function new(Image image) {
		super()
		//this.dreid = new DreiD();
		this.image = image
	}
	
	public function DreiD getDreiD() {
		return this.dreid
	}
	*/
	/**
	 * X, Y and Z offset of the image.
	 */
	//public var offset:Vector3D = new Vector3D(0, 0, 0);
	
	/**
	 * X scale of the image.
	 */
	//private var scale3D:Float;
	
	/**
	 * X scale of the image.
	 */
	//private var angle3D:Float;
	
//}
